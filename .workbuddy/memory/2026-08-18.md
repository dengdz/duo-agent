# 2026-08-18

## 工作记录

- 修复并验证了 `DeepSeekAdapter.parseChunk()` 的工具调用解析。
  - 从 `choices[0].delta.tool_calls[0]` 嵌套结构正确提取 id/name/arguments，避免误取消息级 id。
  - 每轮 `stream()` 前重置解析状态，防止同一适配器实例复用时状态泄漏。
  - 新增无需真实 API 的单元测试，覆盖工具调用和普通文本两种场景。
- 新增三个工具实现：
  - `BashTool`：执行 shell 命令，支持 cwd/timeout，返回 stdout + 退出码。
  - `FileReadTool`：读取文本文件，限制 200KB。
  - `FileWriteTool`：覆盖写入文本文件，自动创建父目录。
  - 每个工具均补充了单元测试。
- 更新 `HANDOFF.md`：标记工具调用修复完成，补充新工具模块，更新测试统计为 67 tests / 0 failures / 3 skipped（缺 API key）。
- 提交：
  - `8aa69f9 fix(deepseek): 修复并验证工具调用解析`
  - `2500c67 feat(tools): 添加 bash、file_read、file_write 工具`
  - `9d6477f docs: 更新 HANDOFF.md 状态与测试统计`

## 待办

- 真实 DeepSeek API 回归测试（需要 `DEEPSEEK_API_KEY`）。
- agent/pre-step/request/turn-stopping 拦截器。
- Session 持久化（JSONL/SQLite）。
